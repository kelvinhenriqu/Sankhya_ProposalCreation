services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/kelvinhenriqu/sankhya-proposal-creation:0.1.5
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      PDF_CONVERTER: libreoffice
      LIBREOFFICE_EXECUTABLE: /usr/bin/libreoffice
      TEMPLATES_DIR: /app/Templates
      PDF_CONVERSION_TIMEOUT: "120"
      HOME: /tmp
      TMPDIR: /tmp
      XDG_CACHE_HOME: /tmp/.cache
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - type: bind
        source: ${TEMPLATES_HOST_DIR:-./Templates}
        target: /app/Templates
        read_only: true
    read_only: true
    tmpfs:
      - /tmp:size=512m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()"
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3
